worker_processes  1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name ddb-react.docker;

        location / {
            proxy_pass http://localhost:57021;
        }
    }
}
