query getMaterial($pid: String!) {
  work(pid: $pid) {
    ...WorkMedium
  }
}

query getInfomedia ($id: String!) {
  infomedia(id: $id) {
    error
    article {
      headLine
      text
    }
  }
}
