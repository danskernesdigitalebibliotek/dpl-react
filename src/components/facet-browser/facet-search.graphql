query searchFacet(
  $q: SearchQuery!
  $facets: [FacetField!]!
  $facetLimit: Int!
) {
  search(q: $q) {
    facets(facets: $facets) {
      name
      values(limit: $facetLimit) {
        term
        score
      }
    }
  }
}
